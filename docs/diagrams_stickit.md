# Diagrams (Mermaid, auto-generated)

Generate locally:
```bash
make -f Makefile diagrams
```

## Class Diagram
```{mermaid}
graph TD
  A[Placeholder] --> B[Run `make -f Makefile uml`]
```

## Call Graph
```{mermaid}
graph TD
  A[Placeholder] --> B[Run `make -f Makefile callgraph`]
```
